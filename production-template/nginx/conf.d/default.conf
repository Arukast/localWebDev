# Nginx configuration template has moved to ../templates/default.conf.template
# The Nginx Docker container automatically processes files in /etc/nginx/templates/*.template
# using envsubst to substitute environment variables such as ${NGINX_WEB_ROOT}.
